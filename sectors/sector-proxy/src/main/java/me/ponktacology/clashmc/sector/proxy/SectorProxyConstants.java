package me.ponktacology.clashmc.sector.proxy;

import java.util.concurrent.TimeUnit;

public class SectorProxyConstants {

    public static final long JOIN_THROTTLE = TimeUnit.MILLISECONDS.convert(20, TimeUnit.SECONDS);

}
